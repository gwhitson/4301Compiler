program stage1no161;
const
  a = true;
  b = false;
  c = not false;
  d = not true;
  e = 5;
  f = +5;
  g = -5;
  h = g;
var
  i, j : integer;
  k : boolean;
begin
  read(i) 
  ; read(j);
  write(i, j);
  k := not (a = c); {legal}
  k := not true; {legal}
  k := not c; {legal}
  k := not and; {expected '(', boolean, or non-keyword id}
end.
